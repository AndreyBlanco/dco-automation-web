# Optional: point the UI at the Python verification service (Playwright + Excel).
# When unset, the app uses the in-browser stub (InsuranceVerificationService).
# VITE_VERIFICATION_API_URL=http://localhost:8000
